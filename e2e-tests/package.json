{
  "name": "presuite-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for PreSuite ecosystem",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:oauth": "playwright test tests/oauth",
    "test:presuite": "playwright test tests/presuite",
    "test:premail": "playwright test tests/premail",
    "test:predrive": "playwright test tests/predrive"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "dotenv": "^16.4.0"
  }
}
